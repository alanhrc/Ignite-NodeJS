// representa um espaço na memória do computador
// usados para tratar os dados e depois remover
// mais performático (informação binária em hexadecimal)

const buf = Buffer.from('ok')

console.log(buf); // hexadecimal
console.log(buf.toJSON()); // decimal
console.log(buf.toString());
